def freqCount(lis):
    freq = {}

    for num in lis:
        if num in freq:
            freq[num] += 1
        else:
            freq[num] = 1
    return freq

ls =[0,0,0,2,1,2,1,1,2,4,4,5,6,7]

print(freqCount(ls))